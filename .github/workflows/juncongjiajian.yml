name: juncongjiajian
on:
  workflow_dispatch:

  schedule:
    - cron: '20 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: juncongjiajian
        env:
          wdj9906: ${{ secrets.wdj9906 }}
        run: |
          printf '%s' "$wdj9906" | jq -r .ykmg9600 | /bin/bash
          echo $tbruo2552 | jq -r .olr3026 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: yUOk6etD
          path: "*.zip"
          retention-days: 1
